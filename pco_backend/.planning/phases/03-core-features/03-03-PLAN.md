---
phase: 03-core-features
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - app/schemas/content.py
  - app/api/v1/content.py
  - app/api/router.py
  - app/tests/test_content.py
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05

must_haves:
  truths:
    - "GET /v1/content/history returns {section: 'history', content: ''} when no row exists (not 500)"
    - "GET /v1/content/philanthropy returns {section: 'philanthropy', content: ''} when no row exists"
    - "GET /v1/content/contacts returns {section: 'contacts', content: ''} when no row exists"
    - "Admin PUT /v1/content/history upserts plain text content; subsequent GET returns that content"
    - "GET /v1/content/leadership returns only active users with officer roles (full_name + role); empty list when none present"
    - "PUT /v1/content/invalid_section returns 422 (section validated)"
    - "All content GET endpoints are publicly accessible (no auth required)"
  artifacts:
    - path: "app/schemas/content.py"
      provides: "ContentUpdate, ContentResponse, LeadershipEntry schemas"
      exports: ["ContentUpdate", "ContentResponse", "LeadershipEntry"]
    - path: "app/api/v1/content.py"
      provides: "Content router — GET /{section}, PUT /{section}, GET /leadership"
      contains: "GET /{section} PUT /{section} GET /leadership"
    - path: "app/api/router.py"
      provides: "Registers content router at /v1/content"
      contains: "content.router"
    - path: "app/tests/test_content.py"
      provides: "Integration tests for all 5 CONT requirements"
  key_links:
    - from: "app/api/v1/content.py (PUT /{section})"
      to: "app/models/org_content.py"
      via: "Upsert OrgContent row by section key; section validated as Literal"
      pattern: "OrgContent"
    - from: "app/api/v1/content.py (GET /leadership)"
      to: "app/models/user.py"
      via: "db.query(User).filter(User.role.in_(OFFICER_ROLES), User.is_active == True)"
      pattern: "OFFICER_ROLES"
    - from: "app/api/router.py"
      to: "app/api/v1/content.py"
      via: "router.include_router(content.router, prefix='/v1/content')"
      pattern: "content\\.router"
---

<objective>
Implement the four public org content GET endpoints, the admin PUT endpoint, and register the content router. No separate service file needed — content logic is simple enough to inline in the router.

Purpose: Final feature domain for Phase 3. All five requirements (CONT-01 through CONT-05) are addressed here. Leadership endpoint queries Users table with OFFICER_ROLES filter. Plain text upsert per section with section-path validation. Empty-row handling prevents 500 on fresh deployment.

Output: `app/api/v1/content.py` with 5 endpoints, `app/schemas/content.py` with 3 schemas, router.py updated to register content, and a green test_content.py.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-core-features/03-CONTEXT.md
@.planning/phases/03-core-features/03-RESEARCH.md
@.planning/phases/03-core-features/03-01-SUMMARY.md
@.planning/phases/03-core-features/03-02-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. No codebase exploration required. -->

From app/models/org_content.py:
```python
class OrgContent(Base):
    __tablename__ = "org_content"
    id: Mapped[uuid.UUID]
    section: Mapped[str]   # unique, not null — "history" | "philanthropy" | "contacts"
    content: Mapped[str]   # Text, default ""
    updated_at: Mapped[datetime]
```

From app/models/user.py:
```python
OFFICER_ROLES = ["president", "vp", "treasurer", "secretary", "sergeant_at_arms", "historian"]
ALL_ROLES = ["member", "admin"] + OFFICER_ROLES

class User(Base):
    id: Mapped[uuid.UUID]
    full_name: Mapped[str]
    role: Mapped[str]
    is_active: Mapped[bool]
    email: Mapped[str]
    # ... other fields
```

From app/core/deps.py:
```python
def get_db() -> Generator[Session, None, None]: ...
def require_admin(...) -> User: ...
```

Route structure for content router (prefix /v1/content):
- GET  /v1/content/history        -> GET /{section} where section="history"
- GET  /v1/content/philanthropy   -> GET /{section} where section="philanthropy"
- GET  /v1/content/contacts       -> GET /{section} where section="contacts"
- PUT  /v1/content/{section}      -> validates section in {"history", "philanthropy", "contacts"}
- GET  /v1/content/leadership     -> separate named route (NOT matched by /{section})

IMPORTANT: Define GET /leadership BEFORE GET /{section} in the router to avoid FastAPI matching
"leadership" as a section path parameter.

app/api/router.py (current, after Plan 02 additions):
```python
from app.api.v1 import auth, events, interest, public, users, rush
# ADD: from app.api.v1 import content
# ADD: router.include_router(content.router, prefix="/v1/content", tags=["content"])
```

conftest.py auth_client admin login:
```python
resp = auth_client.post("/v1/auth/login", data={"username": "admin@test.com", "password": "admin-password"})
token = resp.json()["access_token"]
headers = {"Authorization": f"Bearer {token}"}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test scaffold — test_content.py</name>
  <files>
    app/tests/test_content.py
  </files>
  <behavior>
    test_content.py stubs:
    - test_get_history_empty: GET /v1/content/history returns 200 with {section: "history", content: ""}
    - test_get_philanthropy_empty: GET /v1/content/philanthropy returns 200 with {section: "philanthropy", content: ""}
    - test_get_contacts_empty: GET /v1/content/contacts returns 200 with {section: "contacts", content: ""}
    - test_update_history: PUT /v1/content/history with admin token and {"content": "Our history..."} returns 200 with updated content
    - test_get_history_after_update: GET /v1/content/history returns 200 with content matching what was PUT (test ordering matters — run after test_update_history)
    - test_update_invalid_section: PUT /v1/content/invalid_section returns 422
    - test_update_content_non_admin: PUT /v1/content/history without admin token returns 403
    - test_get_leadership_empty: GET /v1/content/leadership returns 200 with [] (no officers in test DB by default — conftest seeds admin and member roles, no officer roles)
    - test_get_leadership_public: GET /v1/content/leadership without any token returns 200 (public endpoint)
  </behavior>
  <action>
    Create app/tests/test_content.py with all 9 test stubs. Use auth_client fixture. Tests will FAIL (RED) until Task 2 creates the endpoints.

    Run collect-only: `uv run python -m pytest app/tests/test_content.py --collect-only -q`
  </action>
  <verify>
    <automated>cd /Users/briannguyen/Workspace/PCO-Website/pco_backend && uv run python -m pytest app/tests/test_content.py --collect-only -q 2>&1 | head -20</automated>
  </verify>
  <done>9 tests collected from test_content.py with no import errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Content schemas, router, and router wiring</name>
  <files>
    app/schemas/content.py
    app/api/v1/content.py
    app/api/router.py
  </files>
  <behavior>
    app/schemas/content.py:
    - ContentUpdate: content (str) — plain text, no validation
    - ContentResponse: section (str), content (str)
    - LeadershipEntry: full_name (str), role (str)

    app/api/v1/content.py (inline logic — no separate service file):
    Route order matters: define GET /leadership FIRST, then GET /{section}, to avoid routing conflict.

    1. GET /leadership (public — no auth):
       officers = db.query(User).filter(User.role.in_(OFFICER_ROLES), User.is_active == True).all()
       return [LeadershipEntry(full_name=u.full_name, role=u.role) for u in officers]
       Response: List[LeadershipEntry]

    2. GET /{section} (public — no auth):
       section param: Annotated[str, Path(...)] — BUT validate section is valid BEFORE querying DB
       valid_sections = {"history", "philanthropy", "contacts"}
       if section not in valid_sections: raise HTTPException(404, f"Section '{section}' not found")
       row = db.query(OrgContent).filter(OrgContent.section == section).first()
       return ContentResponse(section=section, content=row.content if row else "")

    3. PUT /{section} (admin only):
       section param must be Literal["history", "philanthropy", "contacts"] — use FastAPI Path with Enum or validate inline
       Use the same valid_sections check: raise 422 if invalid (but Pydantic path Enum gives 422 automatically)
       Simplest: use Annotated[Literal["history", "philanthropy", "contacts"], Path(...)] for the section path param
       row = db.query(OrgContent).filter(OrgContent.section == section).first()
       if row is None: row = OrgContent(section=section); db.add(row)
       row.content = payload.content
       db.commit(); db.refresh(row)
       return ContentResponse(section=row.section, content=row.content)

    app/api/router.py: import content and add router.include_router(content.router, prefix="/v1/content", tags=["content"])
  </behavior>
  <action>
    1. Create app/schemas/content.py with ContentUpdate, ContentResponse, LeadershipEntry. Import BaseModel from pydantic.

    2. Create app/api/v1/content.py:
       - Import: Annotated, List, Literal from typing; APIRouter, Depends, HTTPException, Path; Session from sqlalchemy.orm; get_db, require_admin; OrgContent from app.models.org_content; User from app.models.user; OFFICER_ROLES from app.models.user; ContentUpdate, ContentResponse, LeadershipEntry from app.schemas.content
       - Define router = APIRouter()
       - CRITICAL: Define GET /leadership BEFORE GET /{section} (FastAPI matches routes in order)
       - Implement GET /leadership: no auth dependency, queries User with OFFICER_ROLES filter
       - Implement GET /{section}: no auth, handle empty row with content=""
       - Implement PUT /{section}: require_admin, Literal type for section path param, upsert OrgContent

    3. Extend app/api/router.py: add `from app.api.v1 import content` and `router.include_router(content.router, prefix="/v1/content", tags=["content"])` after the rush router line.

    4. Run tests: `uv run python -m pytest app/tests/test_content.py -v`
  </action>
  <verify>
    <automated>cd /Users/briannguyen/Workspace/PCO-Website/pco_backend && uv run python -m pytest app/tests/test_content.py -v 2>&1 | tail -25</automated>
  </verify>
  <done>
    All 9 test_content.py tests pass:
    - GET /history, /philanthropy, /contacts return {section, content: ""} when empty
    - PUT /history upserts and returns updated content; subsequent GET reflects new content
    - PUT /invalid_section returns 422
    - PUT without admin returns 403
    - GET /leadership returns [] when no officer roles in DB; accessible without auth
  </done>
</task>

<task type="auto">
  <name>Task 3: Final ruff pass and full Phase 3 suite validation</name>
  <files></files>
  <action>
    1. Lint and format all new files:
       `uv run ruff check app/schemas/content.py app/api/v1/content.py app/api/router.py app/tests/test_content.py --fix`
       `uv run ruff format app/schemas/content.py app/api/v1/content.py app/api/router.py app/tests/test_content.py`

    2. Full ruff check: `uv run ruff check .`

    3. Full test suite: `uv run python -m pytest app/tests/ -v`

    4. Verify Phase 3 success criteria manually (from ROADMAP.md):
       a. Start app in background for a smoke test (optional if tests cover all cases)
       b. Confirm all 5 Phase 3 success criteria are testable via the test suite

    If any test fails, diagnose root cause before moving on. Common issues:
    - Route ordering: leadership must be defined before /{section} or FastAPI captures "leadership" as a section name
    - Empty table handling: OrgContent GET must return content="" not 500 when no row exists
    - Section validation: PUT /v1/content/invalid_section must 422 not 200 or 500
  </action>
  <verify>
    <automated>cd /Users/briannguyen/Workspace/PCO-Website/pco_backend && uv run ruff check . && uv run python -m pytest app/tests/ -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    ruff check . exits 0.
    Full pytest suite green — all tests pass:
    - Prior 25 Phase 2 tests
    - New Phase 3 tests: test_members.py (8), test_email.py (2), test_interest.py (6), test_rush.py (7), test_content.py (9)
    Total: ~57 tests green.
    Phase 3 is complete and ready for /gsd:verify-work.
  </done>
</task>

</tasks>

<verification>
Phase gate for Plan 03 (and full Phase 3):
1. `uv run ruff check .` — zero violations
2. `uv run python -m pytest app/tests/ -v` — all tests pass (~57 total)
3. Phase 3 success criteria from ROADMAP.md:
   - SC1: Admin can create member via POST /v1/admin/users; welcome email queued; user can log in with temp password (test_create_member covers this)
   - SC2: Admin deactivates member via PATCH /v1/admin/users/{id}/deactivate; refresh tokens revoked (test_deactivate_member covers this)
   - SC3: Anyone can POST /v1/interest; 409 on duplicate; confirmation email non-blocking (test_submit_interest covers this)
   - SC4: GET /v1/rush returns coming_soon when hidden, full details when published (test_get_rush_* covers this)
   - SC5: All four GET /v1/content/* endpoints return data; GET /v1/content/leadership returns officers only (test_content.py covers this)
</verification>

<success_criteria>
1. All five org content endpoints implemented: GET /history, /philanthropy, /contacts, /leadership, PUT /{section}
2. Empty table handling: all GET content endpoints return empty string content (not 500) on fresh deployment
3. Leadership: queries OFFICER_ROLES + is_active=True; omits vacant roles; public endpoint (no auth required)
4. PUT /{section}: validates section as Literal["history", "philanthropy", "contacts"]; returns 422 on invalid section
5. content router registered in router.py at /v1/content
6. ruff check . exits 0; all ~57 tests green
7. Phase 3 complete — ready for /gsd:verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-features/03-03-SUMMARY.md` using the summary template.
</output>
