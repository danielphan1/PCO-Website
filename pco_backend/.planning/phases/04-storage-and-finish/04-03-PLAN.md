---
phase: 04-storage-and-finish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .env.example
autonomous: true
requirements:
  - XCUT-06

must_haves:
  truths:
    - "README exists with setup instructions covering Docker workflow and .env.example reference"
    - "README contains full environment variable reference table with all Settings fields"
    - "README contains architecture overview paragraph explaining the stack and structure"
    - "README links to /docs (FastAPI auto-generated OpenAPI)"
    - "README contains endpoint table with method, path, auth required, and one-line description"
    - ".env.example mirrors all Settings fields with placeholder values"
  artifacts:
    - path: "README.md"
      provides: "Project documentation for future developers"
      contains: "docker compose up"
    - path: ".env.example"
      provides: "Environment variable template"
      contains: "JWT_SECRET"
  key_links:
    - from: "README.md"
      to: ".env.example"
      via: "Setup section references .env.example"
      pattern: "\\.env\\.example"
    - from: "README.md"
      to: "/docs"
      via: "Link to FastAPI Swagger UI"
      pattern: "/docs"
---

<objective>
Write README.md and .env.example. Both files are fully independent of the storage implementation
and can be written in Wave 1 in parallel with Plan 01.

Purpose: Satisfy XCUT-06 — project documentation for a future developer taking over the project.
Output: README.md with five required sections plus endpoint table; .env.example with all env vars.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-storage-and-finish/04-CONTEXT.md

<interfaces>
<!-- Environment variables from app/core/config.py Settings class -->
```python
class Settings(BaseSettings):
    env: str = "dev"
    app_name: str = "psi-chi-omega-api"
    app_version: str = "0.1.0"
    cors_origins: str = "http://localhost:3000"
    database_url: str = "postgresql+psycopg://postgres:postgres@localhost:5432/pco"
    jwt_secret: str  # REQUIRED — no default; min 32 chars
    jwt_alg: str = "HS256"
    access_token_expire_minutes: int = 60
    refresh_token_expire_days: int = 30
    supabase_url: str = ""
    supabase_service_key: str = ""
    smtp_host: str = ""
    smtp_port: int = 587
    smtp_user: str = ""
    smtp_password: str = ""
    frontend_url: str = "http://localhost:3000"
```

<!-- Endpoint inventory for table (from phases 1-4) -->
Auth:
  POST /v1/auth/login — public — Login with email/password, returns JWT access token + refresh token
  POST /v1/auth/refresh — public — Exchange refresh token for new access token

Users:
  GET /v1/users/me — authenticated — Get own profile

Events:
  GET /v1/events — authenticated — List event PDFs with signed download URLs (newest first)
  POST /v1/admin/events — admin — Upload event PDF (multipart/form-data; max 10 MB; PDF only)
  DELETE /v1/admin/events/{id} — admin — Delete event PDF from storage and database

Members:
  GET /v1/admin/users — admin — List all members (filter by active/deactivated)
  POST /v1/admin/users — admin — Create member account (generates temp password, sends welcome email)
  PATCH /v1/admin/users/{id}/role — admin — Update member role (writes to audit_log)
  PATCH /v1/admin/users/{id}/deactivate — admin — Deactivate member (invalidates all refresh tokens)
  PATCH /v1/admin/users/{id}/reactivate — admin — Reactivate member

Interest Form:
  POST /v1/interest — public — Submit interest form (duplicate email returns 409; sends confirmation email)
  GET /v1/interest — admin — List all interest submissions

Rush Info:
  GET /v1/rush — public — View rush info (full details if published, or {"status": "coming_soon"})
  PUT /v1/rush — admin — Update rush info
  PATCH /v1/rush/visibility — admin — Toggle rush info published/hidden

Org Content:
  GET /v1/content/history — public — View org history
  GET /v1/content/philanthropy — public — View philanthropy info
  GET /v1/content/contacts — public — View contact info
  GET /v1/content/leadership — public — View leadership (officer roles from users table)
  PUT /v1/content/{section} — admin — Update content section (history | philanthropy | contacts)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md</name>
  <files>README.md</files>
  <action>
    Create README.md in the project root. Audience: future developer taking over the project.
    Tone: concise and practical, not a tutorial. No diagrams required.

    Required sections (from CONTEXT.md decisions and XCUT-06):

    ## PCO San Diego Backend API

    One-sentence description of what this is (FastAPI REST API for a fraternity chapter backend).

    ## Setup

    Docker workflow only. Steps:
    1. Copy .env.example to .env and fill in required values
    2. docker compose up
    3. API available at http://localhost:8000; docs at http://localhost:8000/docs

    Note: JWT_SECRET is required (no default). Supabase and SMTP credentials are optional for local
    development but required in production.

    ## Environment Variables

    Table with columns: Variable | Required | Default | Description
    Include ALL Settings fields:
    - ENV | No | dev | Runtime environment (dev/prod)
    - APP_NAME | No | psi-chi-omega-api | Application name
    - APP_VERSION | No | 0.1.0 | Application version
    - CORS_ORIGINS | No | http://localhost:3000 | Comma-separated allowed origins
    - DATABASE_URL | No | postgresql+psycopg://postgres:postgres@localhost:5432/pco | SQLAlchemy database URL
    - JWT_SECRET | YES | — | JWT signing secret (min 32 characters)
    - JWT_ALG | No | HS256 | JWT signing algorithm
    - ACCESS_TOKEN_EXPIRE_MINUTES | No | 60 | Access token lifetime in minutes
    - REFRESH_TOKEN_EXPIRE_DAYS | No | 30 | Refresh token lifetime in days
    - SUPABASE_URL | No | — | Supabase project URL (required for event PDF uploads)
    - SUPABASE_SERVICE_KEY | No | — | Supabase service role key (required for event PDF uploads)
    - SMTP_HOST | No | — | SMTP server hostname (required for email sending)
    - SMTP_PORT | No | 587 | SMTP server port
    - SMTP_USER | No | — | SMTP username / sender address
    - SMTP_PASSWORD | No | — | SMTP password
    - FRONTEND_URL | No | http://localhost:3000 | Frontend origin for email links

    ## Architecture

    One paragraph covering:
    - FastAPI + SQLAlchemy (synchronous) on PostgreSQL
    - Alembic for migrations
    - PyJWT for authentication; bcrypt for password hashing
    - Supabase Storage for event PDF files (private bucket; signed URLs for access)
    - aiosmtplib for non-blocking SMTP email via FastAPI BackgroundTasks
    - Code structure: app/api/ (routers), app/services/ (business logic), app/models/ (ORM),
      app/schemas/ (Pydantic), app/storage/ (Supabase wrapper), app/core/ (config, deps, security)

    ## API Reference

    Link: Full interactive docs at http://localhost:8000/docs (Swagger UI)

    Endpoint summary table with columns: Method | Path | Auth | Description
    Include all ~20 endpoints from the interfaces block above. Group by resource area
    (Auth, Users, Events, Members, Interest Form, Rush Info, Org Content).

    Auth values: public / member / admin
  </action>
  <verify>
    <automated>cd /Users/briannguyen/Workspace/PCO-Website/pco_backend && python -c "
content = open('README.md').read()
checks = [
    ('docker compose up', 'Setup section'),
    ('.env.example', 'env.example reference'),
    ('JWT_SECRET', 'env var table'),
    ('SUPABASE_URL', 'Supabase env var'),
    ('Architecture', 'Architecture section'),
    ('/docs', 'docs link'),
    ('GET', 'endpoint table'),
    ('POST', 'endpoint table methods'),
]
for text, label in checks:
    assert text in content, f'MISSING: {label} (looked for: {text!r})'
print('README.md checks passed')
"</automated>
  </verify>
  <done>README.md exists; automated check confirms all required sections and content present</done>
</task>

<task type="auto">
  <name>Task 2: Write .env.example</name>
  <files>.env.example</files>
  <action>
    Create .env.example in the project root. It mirrors all Settings fields with placeholder values.
    Placeholders for required fields (no default): use descriptive strings like "your-jwt-secret-minimum-32-characters-here"
    Placeholders for optional fields: use empty string or descriptive placeholder.

    Format: standard .env key=value with comment lines for clarity.

    Content:
    ```
    # Application
    ENV=dev
    APP_NAME=psi-chi-omega-api
    APP_VERSION=0.1.0
    CORS_ORIGINS=http://localhost:3000

    # Database
    DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/pco

    # Authentication — JWT_SECRET is REQUIRED (minimum 32 characters)
    JWT_SECRET=your-jwt-secret-minimum-32-characters-here
    JWT_ALG=HS256
    ACCESS_TOKEN_EXPIRE_MINUTES=60
    REFRESH_TOKEN_EXPIRE_DAYS=30

    # Supabase Storage (required for event PDF upload/delete)
    SUPABASE_URL=https://your-project.supabase.co
    SUPABASE_SERVICE_KEY=your-supabase-service-role-key

    # SMTP Email (required for welcome and confirmation emails)
    SMTP_HOST=smtp.example.com
    SMTP_PORT=587
    SMTP_USER=your-email@example.com
    SMTP_PASSWORD=your-smtp-password

    # Frontend
    FRONTEND_URL=http://localhost:3000
    ```
  </action>
  <verify>
    <automated>cd /Users/briannguyen/Workspace/PCO-Website/pco_backend && python -c "
content = open('.env.example').read()
required = ['JWT_SECRET', 'DATABASE_URL', 'SUPABASE_URL', 'SUPABASE_SERVICE_KEY', 'SMTP_HOST', 'SMTP_PORT', 'SMTP_USER', 'SMTP_PASSWORD', 'FRONTEND_URL', 'CORS_ORIGINS']
for var in required:
    assert var in content, f'MISSING env var: {var}'
print('.env.example checks passed')
"</automated>
  </verify>
  <done>.env.example exists; all Settings fields present with placeholder values; automated check passes</done>
</task>

</tasks>

<verification>
After all tasks:
- README.md contains: setup instructions, env var table, architecture paragraph, /docs link, endpoint table
- .env.example contains all Settings fields
- Both files are committed
</verification>

<success_criteria>
- Future developer can follow README to start the project with `docker compose up` after filling .env from .env.example
- All env vars documented with Required/Default/Description
- Architecture section explains the stack and code layout
- Endpoint table covers all ~20 v1 endpoints with auth requirements
- XCUT-06 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-storage-and-finish/04-03-SUMMARY.md`
</output>
