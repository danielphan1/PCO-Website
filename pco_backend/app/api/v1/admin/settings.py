"""Admin settings endpoints.

Placeholder router — interest open/close state is now managed via
the rush info visibility endpoint (PATCH /v1/rush/visibility).
"""

from fastapi import APIRouter

router = APIRouter()
